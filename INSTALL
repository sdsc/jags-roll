This roll bundles the JAGS Bayesian model analyzer.  To install, execute these
instructions on a Rocks development machine (e.g., a frontend), starting in
this directory:

% make
% rocks add roll *.iso
% rocks enable roll jags
% cd /export/rocks/install
% rocks create distro
% rocks run roll jags | bash

In addition to the software itself, the roll installs JAGS environment module
files in /opt/modulefiles/applications/jags.
